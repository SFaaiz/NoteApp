<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="iconTint">@color/white</item>
    </style>

    <style name="Base.Theme.NoteApp.ActionBar" parent="Theme.AppCompat.DayNight">
        <item name="iconTint">@color/white</item>
    </style>

    <style name="Base.Theme.NoteApp.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="iconTint">@color/white</item>
    </style>

    <style name="cardBackground">
        <item name="android:backgroundTint">@color/lightGrey</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>